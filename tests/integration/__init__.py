"""Integration tests for google-contacts-cisco.

Integration tests verify that multiple components work together correctly,
including API endpoints, database operations, and service interactions.

These tests use real HTTP requests (via TestClient) and an in-memory database,
with mocked external dependencies (Google API).
"""
